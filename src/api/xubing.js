/**
 * @describe  存放各种API请求
 * @example GET(url, query, options)
 *          POST(url, body, options)
 */

import {
    GET,
    POST
} from './config/index.js'
import {
    commonUrl
} from '../config.js'
